[project]
name = "perceptron"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "polars>=1.8.2",
]

[project.scripts]
hello = "perceptron:hello"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
